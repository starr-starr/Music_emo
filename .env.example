# Select `mock` for a self-contained development experience. Use `api` only
# when an API server compatible with docs/API_CONTRACT.md is available.
VITE_DATA_SOURCE=mock

# Public browser configuration only. Never put credentials in VITE_* values.
VITE_BASE_URL=/api
